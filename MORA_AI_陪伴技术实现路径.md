# ZHAKA AI 陪伴技术路径

> 面向投资人的技术说明：ZHAKA 如何用可解释的 AI 陪伴逻辑，服务**低电量人群**与**情绪低落、抑郁倾向用户**，并落地为**毛绒树懒智能硬件**。

---

## 1. 我们解决什么问题

目标用户常见状态是：累、焦虑、提不起劲、不想被追问、也听不进「你要振作」。

通用聊天机器人的默认模式往往是：**分析原因、连续提问、给建议、写很长**。对低电量用户，这会加重负担。

**ZHAKA 的产品承诺：**

> 陪你在低电量的日子里，慢一点。

技术上，这意味着系统必须先判断「用户这轮还剩多少力气」，再决定回复长短、问不问、帮不帮、要不要角色化陪伴——**而不是把麦克风直接接到一个大模型上自由发挥**。

ZHAKA 是 **陪伴系统里包含心理支持能力**；不是「心理咨询外面套一只树懒」。不做诊断、不开药方，但在明确风险出现时，能暂时放下角色，给出克制、负责任的安全支持。

---

## 2. 产品形态

| 形态 | 作用 |
|------|------|
| **最终产品** | 毛绒树懒硬件：语音说 → AI 决策 → 语音回；可抱、可按、低负担日常陪伴；支持语音日记沉淀长期记忆 |
| **ZHAKA Lab（已有）** | 内部实验台：用文字验证「怎么陪才对」，调人格与行为节奏，对比模型效果，保证上线前行为可控、可复现 |

**同一套陪伴大脑**先在 Lab 里跑通，再接到硬件的「耳朵」（语音识别）和「嘴巴」（语音播报）。硬件不另写一套人格逻辑，避免体验漂移。

---

## 3. 技术逻辑：先计划，再说话

行业常见路径是「用户一句话 → 大模型直接答」。  
ZHAKA 的路径是 **「用户一句话 → 行为决策 → 再生成」**：

```text
用户表达（文字或语音转写）
        ↓
① 安全预检          是否出现明确风险？紧急时走安全路径，不继续角色表演
        ↓
② 轻量路由          判断本轮「电量」、对话任务、是否适合提问、是否涉及大事等
        ↓
③ 本轮回复计划      写多长、问不问、必须做什么 / 不要做什么
        ↓
④ 世界观与口吻      是否轻轻带出树懒平行感受；可选口吻示范
        ↓
⑤ 组装上下文        人格 + 计划 + 记忆 + 近期对话
        ↓
⑥ 主模型生成        按计划把话说成人话（产品侧将采用流式生成）
        ↓
⑦ 记忆（后台）      记住用户愿意留下的事；不挡「先听到回复」
```

### 3.1 为什么这对低电量 / 抑郁倾向用户重要

| 机制 | 用户侧效果 |
|------|------------|
| **能量档位** | 没力气时回复自动变短、少动作，不硬聊 |
| **每轮最多一问** | 避免连续审问；不想被问时可以不问 |
| **任务模式分流** | 陪伴 / 轻问 / 直接回答 / 给一步 / 庆祝 / 收尾等，各有边界，减少「没求就建议」 |
| **大事首轮压短** | 重大失去类话题先接住，不抢戏、不立刻讲故事 |
| **角色化可控** | 树懒世界观按节奏出现，用户拒绝角色化时能关掉 |
| **安全分层** | 日常口语少误伤；真正紧急时优先安全支持 |

大模型负责「说得像 ZHAKA」；**陪的节奏与边界由行为层立法**，这是体验可复制、可调优的前提。

### 3.2 长期关系：记忆与语音日记

- **日常陪伴记忆**：记住偏好、重要的人与事、有效的陪伴方式；用户可查看、修改。  
- **语音日记**（产品能力）：用户主动「记一段」→ 转写确认 → 摘要与记忆候选 → **用户点头后**才进长期记忆。  
  日记不是把录音丢进聊天，而是低负担的高密度记录入口，支撑日后温和的周期回顾（基于用户已确认内容，不擅自贴诊断标签）。

---

## 4. 硬件上的完整链路：听 → 想 → 说

```text
┌────────────── 毛绒设备 ──────────────┐
│  按键 / 触摸唤醒 · 麦克风 · 扬声器     │
│  （不默认环境偷听）                    │
└──────────┬─────────────────▲─────────┘
           │ 语音上行         │ 语音播报
           ▼                 │
┌────────────── 云端陪伴服务 ───────────┐
│  语音识别（ASR）→ 文本                 │
│        ↓                             │
│  ★ 陪伴行为引擎（与 Lab 同源）★         │
│  安全 → 路由 → 回复计划 → 上下文       │
│        ↓                             │
│  主模型流式生成 → 按句语音合成（TTS）   │
│        → 边生成边播报，可被用户打断     │
│        ↓                             │
│  落库 · 记忆后台更新                   │
└──────────────────────────────────────┘
```

三点投资人需要记住的技术选择：

1. **ASR / TTS 是感官，行为引擎是大脑**——语音模块不能绕过安全与回复计划。  
2. **边生成边播报**——不做「全文写完再整段朗读」，否则首响过慢，不适合实时陪伴。  
3. **可打断**——用户一开口，停播、取消后续合成，符合低负担对话习惯。

---

## 5. 我们现在走到哪、下一步怎么走

| 阶段 | 内容 | 状态 |
|------|------|------|
| **① 陪伴大脑** | 安全、能量、提问克制、回复计划、世界观节奏、结构化记忆；Lab 可调、可回放「这一轮为什么这么回」 | **已建成，持续打磨体验** |
| **② 语音通道** | 识别接入同一套行为引擎；流式生成 + 分片播报；打断；语音日记闭环 | 规划中（可用 App/网页麦先验证） |
| **③ 硬件原型** | 毛绒形态、唤醒与声学、弱网与续航、设备身份与端云协议 | 规划中 |
| **④ 产品化** | 账号与多设备、隐私合规、安全话术与流程、成本与降级、长期回顾 | 规划中 |

演进原则：**先稳定「怎么陪」，再接耳朵和嘴巴，最后进毛绒**——避免硬件先量产、人格逻辑却不可控。

---

## 6. 技术壁垒（相对「套皮聊天机器人」）

1. **行为层与生成层分离**：节奏、提问、安全、角色化可产品化配置，不依赖单次 Prompt 碰运气。  
2. **为低电量交互专门设计的约束**：短回复、少提问、模式边界——直接对应目标人群痛点。  
3. **可解释、可回归**：每一轮决策可记录，便于迭代与质检，适合硬件规模化后的体验治理。  
4. **Lab → 硬件一条大脑**：文字验证过的陪伴逻辑，原样服务语音硬件，降低「两个产品两套人格」的风险。  
5. **记忆与日记的确认机制**：长期关系建立在用户可控的信息上，兼顾陪伴深度与信任。

---

## 7. 一句话总结

**ZHAKA 用「先决策怎么陪、再生成怎么说」的 AI 陪伴引擎，服务没力气被追问、也听不进说教的人；当前已在实验室验证文字陪伴闭环，下一步将同一引擎接到语音识别与边说边播的 TTS，最终落成可抱可说的毛绒树懒硬件。**

---

*本文供投资沟通使用。不构成医疗承诺；ZHAKA 提供陪伴与风险场景下的克制支持，不替代专业心理或医疗服务。具体供应商、硬件方案与合规细节以专项设计为准。*
