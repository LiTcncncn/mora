# Few-shot 模块实装记录（2026-08-28）

在本地 Lab 增加 Few-shot 示例语料模块：样本按当轮输入检索后注入提示词，随配置文件导出导入。首批 24 条语料中 6 条带世界观，经三轮调优后为 31 条 / 11 条世界观（见第六之二至六之四节）。

代码已完成，`npx tsc --noEmit` 通过，92 个单测通过，本地实测正常。**外网需先 build 新代码，再导入配置。**

---

## 一、最终决定

方案阶段留的 6 个问题，除编辑 UI 由策划指定外，其余按下述执行：

| 问题 | 决定 |
|---|---|
| 编辑 UI | **做**。独立页面 `/fewshot`，可增删改查、按世界观筛选、按能量档位预览检索结果 |
| 检索策略 | 关键词匹配，复用 Memory 的 `tokenize`（中文字符 bigram），不引入 embedding |
| 每轮注入条数 | 按能量档位分开：E0=1、E1=2、E2=2、E3=3，设置里可改 |
| 分区默认状态 | 默认开启 |
| 裁剪优先级 | Memory → 历史 → **Few-shot** → 自定义实验分区 → 报错 |
| 同轮世界观上限 | 加了，默认 1 条 |

实装中另外加了两条方案里没有的规则，都是实测暴露出来的问题：

**相关度下限 `minScore`（默认 0.35）。** 首次实测「我今天什么都没干，感觉不配休息」时，第二条选中的是「想被问」，相关度只有 0.25，纯属凑数。这条示例示范的是「要提问」，塞进不相关的场景会把行为带偏。现在低于阈值就不注入——宁可这轮不给示例，也不给一条示范了别的行为的示例。

**能量档位允许相邻一档。** 最初按精确匹配过滤，结果「明天要交东西，怎么办」一条都没命中，因为对应样本标的是 E2 而当轮判成 E1，最该用的示例反而被挡在外面。改成 `|档位差| <= 1` 才可用：样本标注的档位表示它的回复长度对应哪一档，跨一档影响不大，跨两档会把长度带偏。

---

## 二、数据结构

### `data/fewshot.json`（新 store，按 profile 隔离）

| 字段 | 说明 |
|---|---|
| `scene` | 短场景标签，同时参与检索，权重 1.5 |
| `energy` | `any` 或 `E0`–`E3`，表示回复长度对应哪一档 |
| `worldview` | `none` / `L1` 非人类体感 / `L2` 雨林联想 |
| `keywords` | 检索键，权重 1.0 |
| `user` / `reply` | 用户原话与期望回复，权重 0.5（只有 `user` 参与检索） |
| `note` | 修改说明，**只给人看，不进提示词** |
| `enabled` | 停用的样本不参与检索 |

`model_raw`（模型原答）不入库。`MORA_STYLE_CORPUS.md` 第 75 行已明确不要把坏例子给模型，它只在人工改稿时有用，留在文档里即可。

### 检索与注入

1. 过滤：停用 → 档位相差两档以上 → 相关度低于 `minScore`
2. 排序：相关度降序，同分时优先正好标注为当前档位的样本，再按 id 保证结果稳定
3. 取用：受该档位条数上限、世界观条数上限、字符预算三重约束
4. 渲染：只输出 `示例 N / 用户： / 你：`，`scene` 和 `note` 不出现

同一输入两次调用必然选出同一批样本，这一条有单测覆盖。

---

## 三、改了哪些文件

**新增 8 个**：`src/domain/fewshot.ts`、`src/server/fewshot/selector.ts` 及其单测、`src/app/api/fewshot/route.ts`、`src/app/api/fewshot/[id]/route.ts`、`src/app/api/fewshot/preview-selection/route.ts`、`src/app/fewshot/page.tsx`、`src/components/fewshot/fewshot-view.tsx`、`data-seed/fewshot.json`。

**修改 15 个**：

| 文件 | 改动 |
|---|---|
| `domain/prompt.ts` | 加 `few_shot` 分区 id、默认顺序、`fewshot.rendered` 变量 |
| `domain/settings.ts` | `contextSettings` 加 `fewShot` 段，全部字段带 `.default()` |
| `domain/config-bundle.ts` | 加 `fewShotSamples`（可选）与两个统计字段 |
| `domain/run.ts` | 快照加 `selectedFewShotIds`、`fewShotSelectionTrace`，带 `.default([])` |
| `persistence/atomic-json-store.ts` | `StoreFileName` 加 `fewshot` |
| `persistence/repositories.ts` | 加 store、repository、档案删除时的级联清理 |
| `context/renderers.ts` | 加 `renderFewShot()` |
| `context/builder.ts` | 入参、渲染、跳过空分区、裁剪链、快照字段 |
| `orchestration/compare.ts` | 每轮解析一次，各槽位共享同一批示例 |
| `api/context/preview/route.ts` | 同步接线，否则预览与实际发出的提示词不一致 |
| `api/config/import/route.ts` | 统计现有样本数 |
| `config/bundle.ts` | 导出带样本；导入重生成 id |
| `components/settings/settings-view.tsx` | 上下文设置里加 Few-shot 控件 |
| `components/settings/config-transfer.tsx` | 导入预览说明 |
| `components/app-shell/app-shell.tsx`、`lib/labels.ts` | 导航项与中文标签 |

**数据迁移**：`data/prompt-presets.json` 与 `data-seed/prompt-presets.json` 加 `few_shot` 分区；两份 `settings.json` 的 `sectionOrder` 加 `few_shot` 并补 `fewShot` 设置段。`data/fewshot.json` 不用手建，首次读取时会自动从 `data-seed/` 复制。

---

## 四、兼容性

**新增 settings 字段全部带 `.default()`。** `JsonStore.read()` 每次读取都跑严格校验，写成必填会让现有 `data/settings.json` 和 2000 条历史 run 直接读取失败。

**`fewShotSamples` 用 `.optional()` 而不是 `.default([])`。** 这点和方案里写的不一样，实测发现的问题：用 `.default([])` 时，导入一份旧配置会把 24 条语料清成 0。现在的语义是——**字段缺失表示「这份配置不管样本」，导入时原样保留；显式给空数组才表示清空。** 已实测验证：导入旧版 `mora-config` 后样本仍是 24 条。

**改动不可回滚。** `data/prompt-presets.json` 里一旦有了 `few_shot`，旧代码的枚举不认这个值，读取即失败。所以外网必须**先 build 新代码，再导入配置**，顺序不能反。

**`.bak` 只保留一份**，会被下一次写入覆盖。同步前手工整份备份 `data/` 目录。

---

## 五、首批语料

首批 24 条，其中 6 条带世界观，占 25%。分布如下（三轮调优后为 31 条 / 11 条世界观，L1 两条已改写为 L2）：

| 场景 | 条数 | 世界观 |
|---|---|---|
| 累 / 不想动 / 躺平 | 4 | fs-0004 休息时内疚（L2，挂在树上） |
| 自责内耗 / 被说懒 / 身体撑不住 | 4 | fs-0008 身体撑不住（L1，凉杯壁） |
| 想说说 / 要你问他 | 2 | — |
| 只要人在、不想分析 | 2 | fs-0012 只想待着（L2，蛇只是在这里） |
| 明确问怎么办 / 该不该 / 已选定动作 | 3 | — |
| 完成小事 / 好消息 | 2 | fs-0017 分享好消息（L1，不太会热烈庆祝） |
| 怼我 / 嫌被问太多 | 2 | — |
| 口语夸张非危机 | 1 | — |
| 轻幽默 / 日常碎碎念 | 2 | fs-0021 被消息压着（L2，红点会咬人吗） |
| 深夜 | 1 | fs-0023 深夜想下结论（L2，夜里声音变大） |
| 边界（想结束对话） | 1 | — |

世界观样本全部取自 57 条种子库，只取态度不搬原文。**「明确问怎么办」和「边界」两类故意一条世界观样本都不放**——让模型从样本里学到什么时候不该联想，比在提示词里写禁令有效。所有回复都避开了已列入避免句式的表达。

---

## 六、实测结果

检索命中（E1 档）：

| 输入 | 选中 |
|---|---|
| 我今天什么都没干，感觉不配休息 | 休息时内疚（L2, 1.16）+ 疲惫不想动（0.45） |
| 明天要交东西，我现在完全不想开始，怎么办 | 明确问怎么办（1.33）+ 疲惫不想动（0.36） |
| 现在两点半，我越想越觉得这几年全白费了 | 深夜想下结论（L2, 0.74） |
| 我好歹把碗洗了 | 完成小事（1.06） |
| 帮我把这段英文翻译一下 | 无（低于阈值） |

分区位置正确：`style` → `few_shot`（197 字）→ `energy_policy`。一轮总计约 7100 字，`maxTotalChars` 24000，余量充足。

导出导入往返：样本无损；旧版配置导入后样本保留。

真实对话两例：

- 「这周末两天我基本都在床上刷手机，什么正事都没办，心里挺过意不去的」→ 没有照抄示例，但**开头用了「听起来你」**，属于避免句式。这是模型本来就有的倾向，不是 few-shot 引入的。
- 「刚熬完一个通宵把方案交了」→ 正常，无照抄。

有一个需要留意的现象：用**和样本完全相同的原话**测试时，模型会逐字复述该样本的回复。这是 few-shot 的固有特性，真实对话不会命中原句，但攒语料时要避免收录太口号化、容易被整句搬走的句子。

---

## 六之二、第一轮调优：让世界观示范真的起作用

测完一轮发现世界观仍然不显，先做了归因。用 15 句常见输入跑 `preview-selection`，带世界观的样本有 **40%** 的轮次进了提示词——频率不是瓶颈，样本进去了但模型没跟着做。四个原因：

**1. 分区引导语和世界观自相矛盾。** 原文是「学说话方式，**不要照抄内容**」，而雨林联想恰恰属于内容。这句话本意是防逐字复述，但它无差别地把世界观也一起防掉了。已改成拆开表述：具体事件不要挪用，但那种从自己雨林生活里随口带一句的说法是角色说话的一部分，贴得上就换成自己的话带一句。

**2. 两条 L1 样本什么都没示范。** 「凉凉的握着也舒服一点」「慢慢挪到你旁边」读不出非人类视角，模型从中学到的是「温柔」而不是角色。**L1 这一档在样本里不成立**：隐性的东西没法靠举例传递，只能靠人格描述。两条已改写为有明确非人类视角的 L2，现在语料里只有 `none` 和 `L2` 两种。

**3. 关键词误命中。** 「朋友好几周没回我消息」命中了「分享好消息」——中文 bigram 分词让「消息」撞上了「好消息」。场景完全相反。已从该样本关键词里删掉「好消息」，换成「面试过了 / 录取 / offer / 升职」等更长的词。教训：**两三个字的高频词不要进关键词**。

**4. 每轮 2 条示例里只有 1 条带世界观。** 1 比 1 传递不出稳定信号，模糊时模型倾向保守。暂未调整（可选做法是把 E1/E2 注入数提到 3 条，世界观上限仍为 1，比例变 1:2），等这一轮改动的效果观察完再定。

同时补了 4 条 L2，覆盖之前全部落空的场景：原地打转看不出变化、计划被打乱、朋友很久不联系、连续下雨提不起劲。

结果：语料 24 → 28 条，世界观 6 → 10 条（36%，且全为 L2）。同一批 15 句重测，命中 40% → **53%**，且原先的两处错配都已配对正确。

---

## 六之三、第二轮调优：检索打分与情绪类语料

起因是查「我很难过」在情景种子库里对应哪一条，顺带发现检索给它配的示例是**「我那个面试过了！」→「哇，那很好啊」**。

**分词的单字问题。** `tokenize` 与记忆检索共用，同时产出单字和 bigram。「我很难过」只有 4 个字，和「面试过了」共享一个「过」字就能拿到 0.5 分。上一轮删掉「好消息」这个关键词只是治标。现在 few-shot 侧加了一层 `fewShotTokenize`，丢掉单字，中文重合必须落在两个连续的字上（`tokenize` 本身没动，记忆检索不受影响）。

**长输入稀释关键词。** 三项打分都是「占用户输入 token 数的比例」，输入越长单个关键词越不值钱：「我爸上个月走了，我到现在缓不过来」命中「走了」只得 0.077 分，整句一条示例都拿不到。新增 `PHRASE_HIT_WEIGHT`——关键词整条原样出现在输入里就加 0.6 分，与输入长度无关，封顶两条防止靠堆关键词取胜。

**关键词不要用两三个字的高频词**（第六之二节的教训）现在有了配套：短词照样会被 bigram 撞，但整条命中的加分让长关键词更有优势，标注时应优先写「面试过了」而不是「过了」。

**补了情绪类语料。** 种子库按「具体情境」编，漏掉了强度、原因、诉求都不明的直白情绪陈述，few-shot 语料同样是空的。补三条：直说难过（D05 暴雨会先变小）、正在哭（H07 长胳膊伸得慢）、事情太重说不出话（D06 一句话想不出来）。这一档正好是世界观素材最好用的地方——都是不讲道理只给态度的联想。

语料现为 **31 条 / 13 条世界观（42%）**。20 句测试输入全部有示例注入，配对无误配，其中 14 句含世界观样本。

---

## 六之四、第三轮：情绪类语料返工与避免句式自查

第六之三节新加的三条情绪样本，人读着别扭。对着 `personas.json` 的 `avoidPatterns` 一查，三条全部违规：

| 原文 | 踩的规则 |
|---|---|
| 「难过就难过着吧，**我在**」 | `avoidPatterns[6]` 在场承诺（我在这儿／我在呢）；`avoidPatterns[2]` 替对方安排感受（让情绪待着） |
| 「**那就哭**，我不打断」 | `avoidPatterns[10]` 替对方处置情绪（想哭就哭吧） |
| 「你现在什么都不用干，**我在这儿**」 | `avoidPatterns[6]`，逐字命中 |

还有两处不在清单里但违反语言规则的：「我见过的暴雨……是先小一点，再小一点」既是"不承诺事情一定会好"的反例，也把暴雨写成了明喻，撞上"不要把每件事都变成人生道理，雨可以只是雨"；「我不打算用别的话把它盖过去」是解说自己在干什么，做作。

**根本判断：直白情绪陈述这一档不该带世界观。** 「我很难过」没有任何具体内容，任何雨林联想都只能当比喻，而比喻就是道理。这和故意不给「明确问怎么办」「边界」两类放世界观样本是同一个道理——让模型从样本里学到**什么时候不该联想**。「直说难过」和「事情太重说不出话」已改标 `none`；「正在哭」保留 L2，因为长胳膊伸得慢是人格明确允许的**角色化动作**，不是比喻。

语料现为 **31 条 / 11 条世界观（35%）**。

**新增自查步骤：语料改动后要对着 `avoidPatterns` 机械扫一遍 `reply` 字段。** 这次的三条是逐条手写的，仍然溜进去三处违规，靠人眼复核不可靠。扫描时除了清单原文，还要覆盖它们的口语变体（「我在」之于「我在呢」，「那就哭」之于「想哭就哭吧」）以及"承诺会好转"这类清单里没有但人格禁止的表达。已确认其余 28 条干净。

---

## 六之五、批量文本编辑

逐条表单适合改一两条，不适合通读和整批调语气，而语料的日常工作恰恰是后者。**逐条编辑已下线，`/fewshot` 只有一个输入框**，整份语料在里面维护。

格式（`src/domain/fewshot-text.ts`，前后端共用同一份编解码）：

```
[fs-0004]
场景: 休息时内疚
档位: E1
世界观: L2
启用: 是
关键词: 不配休息, 浪费时间
用户: 我今天什么都没干，感觉不配休息。
回复: 干完活才配休息，这规矩是谁定的啊。
备注: 只给人看，不进提示词

---

场景: 新条目不写方括号那行
用户: ...
回复: ...
```

- 记录之间用单独一行 `---` 分隔，方括号里是 id，用来把这段文本认回原样本并**保留 `createdAt`**；不写就是新建。
- 字段值可以换行续写，直到下一个字段名或记录结束。
- 档位和世界观接受 `E1` 也接受界面上那种 `E1 低电量`；启用接受是／否。填错报错并指出行号，不静默改值。
- 删掉一整段就是删除那条样本。**保存按钮在会删记录时变成二次确认**，并写明删几条。

保存走 `PUT /api/fewshot/bulk`，服务端重新解析同一份文本（客户端解析只用于即时报错，不作为写入依据），按 id 归并后整体替换该档案的语料。内容逐字段比对，没变的样本连 `updatedAt` 都不动——否则每保存一次就会刷掉全部 31 条的时间戳。返回新增／修改／删除／未变动四个计数。

实测往返：31 条原样回存得到「未变动 31」，加一条得到「新增 1」，删回去得到「删除 1」，`createdAt` 保持不变。

接口随之收敛：`/api/fewshot` 只剩 GET 列表，`POST` 和整个 `/api/fewshot/[id]` 路由已删除，`fewShotRepository` 的 `get/create/update/remove` 一并移除，只留 `list` 和 `replaceForProfile`。档案删除时的级联清理走 `fewShotStore` 直接过滤，不受影响。

---

## 六之六、体积与冲突评估，以及档位规则收紧

用 `/api/context/preview` 量了真实提示词（E1 一轮，共 7186 字）：

| 分区 | 字数 | 占比 |
|---|---|---|
| persona | 3729 | 51.9% |
| style | 1652 | 23.0% |
| response_contract | 943 | 13.1% |
| energy_policy | 350 | 4.9% |
| safety_baseline | 188 | 2.6% |
| **few_shot** | **149** | **2.1%** |
| memory | 65 | 0.9% |

**体积不是问题。** few-shot 在 141–200 字之间波动，占 2–3%，`maxTotalChars` 24000 用掉不到三成，没命中时整块跳过。真正的重量是 persona 一家占 52%，加上 style 和 response_contract 共 88%。要减重该动那三块，而 few-shot 反而是减重手段——一条 70 字示例能替掉好几条抽象规则。

**但发现一处真实冲突。** E1 轮次注入了两条 E0 样本（15 字和 20 字，各 2 句），而同一份提示词里 `energy_policy` 写着「通常三到五句」「不超过约 280 个字符、5 句」，`response_contract` 又写「不要只点个头」。指令要 3–5 句，示例演示 2 句——**few-shot 的示范力压得过抽象指令**，模型会照示例给两句，而你在提示词里查不到原因。实测跨档注入占 16%。

根因是相邻档位规则允许双向跨一档。但档位标注的语义本来就是「这条的回复长度对应哪一档」，双向跨档等于故意注入长度错误的示范。已改为**只允许向上跨档**：低档样本不进高档轮次（短了没有任何机制能补长），高档样本仍可向下用一档（长了会被能量策略的字数上限压回去）。

收紧后 18 句测试输入命中 18 句（100%），跨档 0 条。没有付出命中率代价——当初加双向跨档是语料只有 24 条时的权宜之计，现在 31 条覆盖够了。顺带修好了一处已知回退：「就想安静待着，别问我」原本被两条 E0 样本占位，现在正确命中 E1 档的「只想待着不想被推动」，世界观样本也回来了。

**剩下两个较轻的风险没动**，留待观察：一是分区引导语每轮都在讲雨林，哪怕本轮注入的示例一条都不带世界观（同「别让静态文案说本该由本轮素材决定的事」）；二是示例的「用户：／你：」格式和 history 分区同构，多轮对话里模型可能把示例里的事当成用户先前说过的，目前靠引导语和两块之间约 5000 字的距离隔开。

---

## 七、还没做的

- **世界观调度器**。目前世界观样本靠相关度自然命中，`worldview` 字段只用于「同轮最多 1 条」的上限控制。要精确控制到 1/4 的频率，还需要按之前讨论的方案做：代码记录最近若干轮的注入状态、加硬闸门（安全 / 明确求办法 / 用户拒绝 / E0）、否决顺延不累积。
- **种子库检索注入（第三档）**。现在的检索层和注入层已经通用，接种子库时可以复用，不用再改分区枚举、白名单和裁剪优先级。
- **语料扩到 50–80 条**。`MORA_STYLE_CORPUS.md` 第 10 节的建议是 50 条可用、100 条较稳。现在 31 条，检索未命中时会一条示例都不给。

---

## 八、同步到外网的步骤

顺序不能反：

1. 手工整份备份服务器上的 `data/` 目录
2. 上传新代码并 rebuild、重启（**这一步必须在导入配置之前**）
3. 本地 `/settings` → 导出当前档案配置
4. 外网 `/settings` → 导入该文件，预览里应显示「写入 31 条 Few-shot 示例」
5. 外网 `/fewshot` 确认 31 条在，`/compare` 复测一轮

与之前几次不同，**这次必须重传源码**：新增了 `few_shot` 分区 id 和 `fewshot` 数据文件，旧代码读不了新数据。
