# MORA 用户能量档说明

给策划看：系统怎么判断用户这轮「还剩多少力气」，以及判到某一档后，陪伴模型被要求怎么回。

能量档**不是心理诊断**，也不是心情标签。它只回答一件事：**这轮回复该有多轻、多重**，避免在对方没电时还长篇分析、连问带建议。

当前 Demo **默认用模型判定**（设置里 Energy 模式为 `llm`）。规则关键词会先算一稿，给判定模型参考；判定失败则退回规则结果。对比页也可以**本轮手动指定档位**，指定后不再走自动判定。

---

## 四档分别是什么

| 档 | 含义（给判定用） | 用户大概什么状态 |
|---|---|---|
| E0 几乎没电 | 很沉、很空、撑不住 | 撑不住、什么都不想做、只想有人在 |
| E1 低电量 | 累，但还能接一句 | 累、烦，但还能看一句短回复 |
| E2 一般 | 还能聊聊 | 愿意说一点，或想轻轻商量 |
| E3 有余力 | 想讨论、想计划 | 想分析、想办法、想把事想清楚 |

判定时另有一条产品约定：**对方在邀约被问、想说说、或抱怨「你怎么不问我」时，不要判成 E0**。没电和「想被接住」不是一回事。

---

## 判定怎么发生（当前默认）

每一轮用户发来一句话，按这个顺序：

1. **本轮手动指定了档位** → 用指定档，结束。
2. **模式是固定档位** → 用设置里的固定档（现在基准档是 E1），不看这句话。
3. **否则先跑规则**，得到一份「草稿档位」。
4. **默认再交给判定模型**（目前是 DeepSeek 的 flash 小模型）：看用户原话 + 规则草稿，按整句语义选出 E0–E3，并写一两句中文理由。
5. 判定模型没返回可用结果，或调用失败 → **用规则草稿**，并在记录里写明回退了。

对比页的 Context / Run 里能看到：最终档位、来源（手动 / 规则 / 模型）、判定理由。

---

## 规则判定看什么

规则只看**这一句原文**，做加减分，**从设置里的基准档（现在是 E1）往上或往下调**。命中多个词就加/减多次。

| 信号 | 现在怎么算 | 作用 |
|---|---|---|
| 低能量词 | 每命中一个词，档位往低推一档（可叠加） | 往 E0 推 |
| 高能量词 | 每命中一个词，档位往高推一档 | 往 E3 推 |
| 句子很短 | 字数 ≤ 12（含标点）再往低推一档 | 「嗯」「累」容易偏低 |
| 疲惫式标点 | `……`、连续句号、`~~~` 等，每次约往低推一点 | 语气拖沓时略偏低 |

高低能量**可以同时命中**。例如「好累但想聊聊怎么办」：低词「累」和高词「想聊聊」「怎么办」会互相拉，再交给判定模型用整句语义拍板。

**当前低能量词：** 累、没电、疲惫、撑不住、不想动、什么都不想做、烦、睡不着、崩溃、算了

**当前高能量词：** 想聊聊、我想说说、想说说、不问问我、为什么不问、你问我、有个计划、想试试、帮我想想、怎么办、分析、准备

规则是机械匹配：句子里出现这些字就计数，**不管反话、引用、开玩笑**。所以默认还要模型看一遍整句。

**例子（只说明规则直觉，最终以模型为准）：**

- 「好累，什么都不想做」→ 低能量词多、句子也可能偏短 → 草稿容易到 E0。
- 「我想说说今天的事」→ 高能量词 → 草稿容易偏高，且判定模型被要求不要判 E0。
- 「烦死了，帮我想想怎么办」→ 两边都有 → 草稿可能在中间，由模型按「还想要办法」往 E2/E3 靠。

这些词和「多短算短」都可以在 **设置 → Energy** 里改，改完下一句生效。

---

## 判定模型看什么

判定模型**不是**陪聊天的那两个模型。它只输出档位和理由，不生成对用户说的话。

它被要求：

- 根据**这一句原话**判断当下还剩多少可用力气
- 禁止诊断、贴病理标签、猜测没说出口的病因
- 规则草稿可能错（反话、玩笑、一个词绑死），**以整句语义为准**
- 想说说 / 想被问 → 不要判 E0
- 只输出档位，不要陪聊

给它的档位定义（和上表一致，并带回复轻重提示）：

- E0：回复必须极短、通常不提问、不建议；但邀约被问时不要判成 E0
- E1：回复短，最多一个很轻的问题或小动作
- E2：可以温和展开一点
- E3：可以更完整地一起想

---

## 判到某一档后，陪聊模型收到什么

档位确定后，会拼进 Prompt 的「能量策略」段。陪聊模型（Kimi / DeepSeek 等）按这段生成回复。

这是**指令，不是事后剪裁**：回长了、问多了，系统只在 Run 里记一笔「和目标有偏差」，**不会改写或截断模型原文**。

四档都会带上这几句结构：气质一句 + 行为说明 + 长度目标 + 最多几个问句 + 能不能给小建议。

### E0 几乎没电

- **气质：** 非常轻、非常短。只需要让对方知道有人在。
- **行为：** 默认只承接、陪伴；不建议行动、不分析。没有表达欲时不要提问。若对方明确想说说、想聊聊，或抱怨你没问他：必须用树懒口吻接住，并只问一个很轻、好答的问题。禁止用「愿意说的时候我听着」「不想说也没关系」把话挡回去。
- **长度：** 大约不超过 80 字、3 句
- **提问：** 最多 1 个
- **建议：** 不要提出任何行动或任务

### E1 低电量

- **气质：** 温和、简短、不着急。
- **行为：** 先承接。最多问一个问题，**或者**提一个非常小、几乎不费力的动作，并允许对方拒绝。不要同时又问又建议。若对方明确想说说或想被问到，优先问一个具体、好答的轻问题，不要只把选择权丢回去。
- **长度：** 大约不超过 120 字、3 句
- **提问：** 最多 1 个
- **建议：** 最多 1 个很小的建议，允许拒绝

### E2 一般

- **气质：** 温和，可以稍微展开一点。
- **行为：** 可以温和探索对方在意的部分，或给出一个具体的小步骤。口语、克制，不要列清单。
- **长度：** 大约不超过 220 字、5 句
- **提问：** 最多 1 个
- **建议：** 最多 1 个，允许拒绝

### E3 有余力

- **气质：** 自然、放松，可以聊得更完整。
- **行为：** 可以更完整地讨论，必要时一起想具体做法。不说教，不用编号清单堆建议。
- **长度：** 大约不超过 500 字、8 句
- **提问：** 最多 2 个
- **建议：** 最多 2 个，允许拒绝

人设、回复约定仍会同时生效。能量档说「最多问 1 个」，人格里「有表达欲要问」也成立，两者应一起读：没电时默认不问，**想说时仍允许那一个轻问**。

---

## 和人设的关系

| 这块 | 管什么 |
|---|---|
| 能量判定 | 这轮算哪一档 |
| 能量策略（上文四档） | 这轮多重、能不能问、能不能建议 |
| 人格 / 回复约定 | 树懒口吻、表达欲例外、禁止的句式 |

只改人设、不改四档说明，模型仍可能按旧的「E0 别问」说话。人设和能量要一起改、不要顶牛。

---

## 策划可以在设置里改什么

路径：**设置 → Energy**

- 判定模式：模型判定（默认）/ 只用规则 / 固定某一档
- 规则的低/高能量词、多短算短
- 每一档的气质、行为说明、长度、问句数、是否允许小建议
- 本轮是否允许在对比页手动改档

改词表只影响**规则草稿**；默认模式下，最终档位仍以判定模型为准。改四档「回复说明」则直接影响陪聊模型每一轮看到的生成规则。

---

## 验收时怎么看

1. 说一句「好累，什么都不想做」：档位应偏低，回复应短、少问、不布置任务。
2. 说「我想说说」或「为什么不问问我」：不应被当成 E0 且全程不问；应接住并轻问一句。
3. 说「这个交接怎么办」：档位应偏高，可以给一个小步骤，仍不要长清单。
4. 打开 Context 预览：核对「当前能量档」和「能量策略」正文是否和预期一致。
