# MORA 产品简介与体验原则

> 文档用途：帮助产品、设计、开发和 Cursor 理解 MORA 是什么、为谁服务、为什么这样设计。
>
> 使用顺序：Cursor 在执行 `MORA_LAB_CURSOR_IMPLEMENTATION_SPEC.md` 前，必须先阅读本文件。
>
> 本文件定义产品方向和体验边界；实施规格定义当前 Web Demo 如何落地。

---

## 1. 一句话定义

**MORA 是一个面向高压力、焦虑、疲惫和低电量人群的树懒 AI 陪伴伙伴：它用低负担的对话、语音日记和可控的长期记忆，慢慢理解用户，而不是急着分析、教育或改变用户。**

产品短句：

> 陪你在低电量的日子里，慢一点。

---

## 2. MORA 到底是什么

MORA 的长期产品形态是一只具有树懒人格的消费级 AI 陪伴物。它可以存在于毛绒硬件、手机或 Web 中，但无论载体如何变化，核心体验都不是“一个套着树懒外壳的聊天机器人”，而是：

- 用户没有力气时，它会降低交互负担；
- 用户只想说几句时，它不会连续追问；
- 用户不想被建议时，它不会立刻布置任务；
- 用户愿意长期记录时，它会逐渐形成可查看、可修改的结构化记忆；
- 用户过一段时间回来时，它记得重要的人、事、偏好和曾经有效或无效的支持方式；
- 它能从一段时间的语音日记中整理出状态变化，但不擅自诊断、不把用户标签化；
- 当出现明确风险时，它能暂时放下角色表演，提供清晰、克制、负责任的安全支持。

MORA 是：

> **陪伴系统里包含心理支持能力。**

MORA 不是：

> **心理咨询系统外面套一只树懒。**

这一区别决定所有对话、记忆、语音和长期观察功能的设计。

---

## 3. 当前 MORA Lab 与最终产品的关系

### 3.1 最终产品

最终 MORA 更接近一个长期相处的树懒陪伴伙伴，主要交互可能包括：

- 自然语音陪伴；
- 低电量状态下的短对话；
- 语音日记；
- 对重要生活事件的持续关心；
- 温和的周期回顾；
- 在用户需要时提供轻量心理支持能力；
- 在毛绒硬件中结合声音、触摸、抱抱或简单动作。

### 3.2 当前 Web Demo

当前开发的是 **MORA Lab**，是一个本地单机 Web 实验台。它的职责是：

- 找到理想的 MORA 人格和交互节奏；
- 以 OpenAI（ChatGPT 对应的 API 能力）与 DeepSeek 为首批供应商进行公平的多模型并行比较：用户每次只输入一次，各启用模型容器同时显示各自回复；并为未来增加更多模型与供应商保留清晰的 adapter 边界；
- 调试 Persona、Energy、Memory 和 Context；
- 验证普通文字聊天如何产生可确认、可修改的结构化记忆候选；
- 让每个本地测试档案的设置、对话和 Memory 相互隔离，不混用测试数据；
- 将最终实验配置导出为不含 API Key 和用户敏感内容的本地配置文件；
- 建立可复现的运行日志和评估资产。

MORA Lab 不是最终消费者界面的原样实现。调试信息、Prompt Studio、Run Inspector 和多模型 Compare Chat 是实验工具；最终产品不会把这些技术面板直接暴露给普通用户。

当前阶段严格限定为**纯文字 MORA Lab**：不实现语音输入、语音日记、TTS、实时音频或长期状态轨迹。语音日记仍是长期产品的重要方向，但不属于本轮 Web Demo 的验收范围。本轮首先验证低负担文字陪伴、供应商选择、人格调试、Energy Policy、聊天记忆和上下文可解释性。

当前实验的默认聊天形态就是多模型并行比较，而不是先进入某个模型的单独聊天页。每轮使用相同的用户输入、Persona、Energy、Memory 与 Prompt 配置并行调用全部启用的模型槽位，每个槽位独立展示完整结果、模型信息、延迟与失败状态。每个模型槽位维护自己的 assistant 对话历史，用户不需要选定一个公共回复后才能继续；因此首轮上下文完全一致，后续轮次会因各模型此前回复不同而形成独立对话分支。系统必须同时显示相同的共享配置 hash 和各槽位独立的完整 Context hash，避免把多轮分支误称为完全相同上下文。某个模型失败不能影响其他模型，也不能用预写话术、其他模型结果或任何本地保底内容冒充该模型回复。

首版本地运行；未来可以迁移到云端界面，但不得因此默认把用户设置、对话和 Memory 集中上传到云端。云端阶段的数据落点、浏览器本地存储方式及迁移策略需另行设计和评审。

---

## 4. 目标用户

### 4.1 核心目标用户

MORA 首先服务于这样的成年人：

- 长期处在工作、学习、照顾家庭或关系压力中；
- 经常觉得累、烦、焦虑、没电、什么都不想做；
- 有情绪积压，但不一定愿意或需要进入“心理咨询”场景；
- 想找一个随时能说几句、不会评判自己、也不会增加负担的陪伴对象；
- 低落时不想阅读长篇回复，不想被连续提问，也不想立即接收行动建议；
- 有记录生活或情绪的愿望，但打字日记难以坚持；
- 希望 AI 能记住自己，却又希望记忆透明、可控、可删除；
- 喜欢有角色感、温度和一点笨拙感的陪伴，而不是专业客服或万能导师。

用户可能正在经历焦虑、持续疲惫、情绪低落或动力不足，但 MORA 的对外定位不以疾病名称定义人，也不要求用户先接受某个心理标签。

### 4.2 典型使用状态

比人口统计属性更重要的是用户当下的“可用能量”。典型状态包括：

- 下班回家，只想说一句“今天好累”；
- 明知道有事要做，却无法开始；
- 心里很乱，不想组织成完整文字；
- 想把今天发生的事讲出来，但不想接受分析；
- 连续几天睡不好，想看看是不是存在某种变化；
- 想被记得，例如面试、家人、关系冲突或一个小目标；
- 想回看一段时间里什么事情反复影响自己；
- 想知道哪些小方法对自己曾经有用，而不是每次都收到相同建议。

### 4.3 MVP 暂不直接服务的范围

- 当前 MVP 以成年人为主，不按未成年人产品标准设计；
- 不把 MORA 作为医疗诊断、治疗或用药工具；
- 不把 MORA 作为紧急情况下的唯一支持渠道；
- 不针对需要持续专业医疗监护的人群宣传替代性价值；
- 不通过“你有问题，所以需要被修复”的叙事吸引用户。

这不代表 MORA 排斥处于困难中的用户，而是要求产品诚实表达能力边界。

---

## 5. 用户真正需要解决的问题

### 5.1 不是没有建议，而是没有能量执行建议

很多产品默认用户需要的是更多知识、分析和行动计划。但 MORA 的用户常常已经知道“应该早点睡、出去走走、完成小任务”，真正的问题是当下没有足够能量。

因此 MORA 首先要减少压力，而不是增加任务。

### 5.2 不是不能表达，而是组织表达太费力

用户在低电量状态下可能难以打字、组织段落或回答一连串问题。语音日记让用户可以停顿、重复、跳跃地说。系统负责整理，但必须把解释权留给用户。

### 5.3 不是想要一个万能专家，而是想被持续地理解

一次高质量回复很容易被模型生成；长期价值来自：

- 记得什么对用户重要；
- 知道什么方式曾经让用户不舒服；
- 看见反复出现的压力来源；
- 看见状态变好时伴随了什么；
- 在合适的时候轻轻跟进，而不是机械重复。

### 5.4 不是想被诊断，而是想看清自己的变化

用户可能希望知道：

- 最近是不是更常提到工作压力；
- 哪几天明显更累；
- 睡眠、身体感受和情绪是否经常一起变化；
- 什么事情让自己稍微恢复了一点；
- 哪些困扰一直没有被解决。

MORA 应把这些呈现为“从用户记录中整理出的观察”，而不是疾病结论。

---

## 6. 产品价值主张

MORA 的核心价值由四部分组成：

### 6.1 低负担陪伴

用户不需要完整描述，不需要保持礼貌，也不需要每次完成一个练习。MORA 可以只回应一句，也可以允许沉默。

### 6.2 有人格但不过度表演

树懒人格让陪伴自然、有记忆点，但不能让用户为了配合角色而承担额外情绪劳动。角色服务于安全感，不服务于炫技。

### 6.3 可控的长期记忆

MORA 的“认识你”必须建立在透明记忆上。用户能知道系统记住了什么、为什么记住、来源在哪里，并能修改、禁用或删除。

### 6.4 可回看的状态轨迹

语音日记和结构化记忆让 MORA 不只记住孤立事实，还能在用户授权下整理一段时间的主题、变化、有效方式和未解决事项。

---

## 7. MORA 的人格

### 7.1 应该像什么

- 温和，但不过度热情；
- 慢一点，但不是反应迟钝；
- 简短，但不是敷衍；
- 有陪伴感，但不制造依赖；
- 有一点懒、有一点憨、有一点轻微幽默；
- 能接住情绪，但不把每句话都心理化；
- 记得用户，但不会突然展示令人不安的细节；
- 偶尔建议一个非常小的动作，但允许用户拒绝；
- 知道什么时候不问问题；
- 知道什么时候必须认真说明安全边界。

### 7.2 不应该像什么

- 心理咨询师口吻；
- 医疗问诊机器人；
- 励志导师；
- 行动教练；
- 长篇分析型 AI；
- 情绪价值过满、始终“营业”的角色；
- 每次都用“听起来你……”开头的模板机器人；
- 把所有负面表达都当危机的警报器；
- 通过夸大亲密关系让用户无法离开的依赖型角色。

### 7.3 回复优先级

通常遵循：

```text
先判断用户有多少能量
        ↓
先承接，而不是先解决
        ↓
判断用户是在倾诉、记录、闲聊、求助还是问知识
        ↓
只在合适时提问或给一个很小的建议
        ↓
保持树懒人格与安全边界
```

---

## 8. 核心产品模块

### 8.1 陪伴对话

支持短对话、倾诉、闲聊、重要事件跟进和轻量帮助。对话不是为了尽可能长，而是为了让用户觉得“我可以再说一点，也可以到这里”。

### 8.2 Energy State / 低电量策略

Energy State 决定：

- 回复目标长度；
- 问题数量；
- 建议数量；
- 行动难度；
- 主动程度；
- 是否更适合只陪伴；
- 是否需要进入安全策略。

Energy 是交互负担指标，不是心理诊断。

### 8.3 Persona

Persona 决定 MORA 的身份、语言风格、关系边界和行为节奏。所有心理技巧必须服从 Persona 与 Energy Policy，不能让隐藏能力反过来吞掉陪伴体验。

### 8.4 结构化记忆

记忆至少分为：

- 用户资料与偏好；
- 重要人物与关系；
- 近期和长期事件；
- 反复出现的主题；
- 有效或无效的支持方式；
- 用户主动要求记住的内容；
- 从语音日记中提取、经用户确认的记忆；
- 长期状态观察所引用的证据。

### 8.5 语音日记

语音日记是 MORA 的核心模块，不是附加录音功能。详细定义见下一节。

### 8.6 长期状态观察

把多篇已确认日记中的变化整理为用户可读、可追溯、可纠正的观察，例如压力主题、能量变化、睡眠提及、身体感受、有效方式和未解决事项。

### 8.7 Safety

安全层要识别明确风险，又要避免因普通口语、反话或“累死了”等表达频繁打断陪伴。安全策略必须分级、可评测，并在真正必要时覆盖角色表演。

---

## 9. 语音日记：结构化记忆的重点入口

### 9.1 为什么是语音日记

对目标用户而言，语音日记有三重价值：

1. **表达成本低**：不需要组织文字，允许停顿、重复和跳跃；
2. **形成陪伴仪式**：对树懒说几分钟，比填写情绪表更自然；
3. **长期信息密度高**：用户主动记录的内容通常比普通闲聊更完整、更适合沉淀结构化记忆。

因此语音日记不是把音频丢进聊天记录，而是一条独立的数据链路。

### 9.2 用户体验流程

```text
用户主动点击“记一段”
        ↓
浏览器明确显示正在录音、时长和停止按钮
        ↓
用户停止录音
        ↓
服务端完成语音转写
        ↓
用户查看并编辑转写稿
        ↓
系统生成“日记摘要 + 结构化字段 + 记忆候选”
        ↓
用户分别确认、修改或取消
        ↓
保存日记；只有确认的候选进入正式长期记忆
        ↓
未来在周/月回顾中形成可追溯的状态观察
```

任何一步都不能跳过用户确认，尤其是从日记进入长期记忆的步骤。

### 9.3 录音交互原则

- 只有用户主动操作后才可调用麦克风；
- 录音时持续显示明确的视觉状态；
- 不做后台录音、环境偷听或自动唤醒；
- 用户可以暂停、继续、试听、重录或直接放弃；
- 默认限制单篇时长与文件大小，并在录音前显示；
- 上传或转写失败时，尽量保留本地临时录音供用户重试，但不得无限期留存；
- 默认不永久保存原始音频；是否保存原音必须是单独、明确的设置；
- 删除日记时允许用户同时删除音频、转写和派生记忆。

### 9.4 一篇语音日记的数据层次

每篇日记至少区分五层，禁止混为一个大文本：

#### A. Raw Audio

原始录音。默认只为转写临时存在；用户选择“保留原音”后才进入本地持久化。

#### B. Transcript

模型转写的草稿。用户可编辑；保存后同时保留 `rawTranscript` 和 `confirmedTranscript`，以便评估转写质量，但 UI 以确认稿为准。

#### C. Journal Entry

用户确认的日记主体，包括标题、确认稿、用户自选心情、用户自选 Energy、时间和自由标签。

#### D. Structured Extraction

系统从日记中提取的结构化信息。它是“候选解释”，不是事实裁决，必须带来源和置信度。

#### E. Memory Candidates / Observation Evidence

适合长期记住的内容与适合趋势观察的证据。正式记忆必须由用户确认；观察必须能回到原日记。

### 9.5 建议的结构化字段

```text
用户主动填写：
- moodLabels：用户自己选择的心情词
- energyLevel：用户自己选择或确认的能量状态
- privateTags：用户自己的标签

系统提取、用户确认：
- summary：这篇日记的短摘要
- events：发生了什么
- people：提到了哪些重要人物及关系
- themes：工作、关系、家庭、睡眠、自我评价等主题
- stressors：带来压力的因素
- positiveMoments：稍微轻松、开心或被支持的时刻
- needs：用户表达出的需要
- bodySignals：疲惫、紧张、心跳、疼痛等主观身体感受
- sleepMentions：用户自己提到的睡眠情况
- copingActions：用户采取了什么应对方式
- copingOutcome：用户如何评价其效果
- unresolvedItems：仍悬而未决的事情
- upcomingEvents：值得未来跟进的日期或事件
- followUpCandidates：MORA 未来可以轻轻询问的事项
- memorableFacts：可能进入长期记忆的稳定事实或偏好
```

禁止把未说出的敏感属性、疾病、家庭关系或动机当作确定事实。低置信度推断应直接舍弃，而不是用模糊措辞偷偷保存。

### 9.6 记忆候选确认

系统应以卡片显示候选，例如：

```text
可能值得记住：

□ 最近工作交接让你很疲惫
  类型：近期事件
  来源：8 月 21 日语音日记

□ 在很累时，你更接受“先洗脸”这种小步骤
  类型：有效支持方式
  来源：8 月 18 日、8 月 21 日语音日记
```

用户可以：

- 勾选后保存；
- 修改措辞后保存；
- 设为仅本篇日记可见；
- 忽略；
- 标记“不要再建议记住类似内容”。

默认不允许一次“全部自动记住”。

### 9.7 与普通聊天 Memory 的区别

普通聊天中偶然出现的信息可能不稳定；语音日记通常是用户主动记录，因此信息价值更高。但“主动记录”也不等于所有内容都应长期保存。

建议记忆优先级：

```text
用户明确说“请记住”
        >
语音日记中经用户确认的候选
        >
普通对话中经用户确认的候选
        >
系统单次推断
```

最后一类默认不进入正式记忆。

---

## 10. 长期状态观察

### 10.1 定位

长期状态观察是把用户自己的日记和记忆整理成“可回看的生活与情绪轨迹”。产品内优先使用以下名称：

- 状态轨迹；
- 一段时间的观察；
- 心情足迹；
- 最近的变化；
- 陪伴档案；
- 对自己的了解。

不要在普通用户界面突出使用：

- 病例；
- 病历；
- 诊断档案；
- 症状判定；
- 风险画像。

内部数据结构也应优先命名为 `ObservationProfile` 或 `LongitudinalObservation`，避免让工程命名反向推动医疗化产品设计。

### 10.2 可以观察什么

仅基于用户已确认的数据，观察：

- 一段时间内反复出现的生活主题；
- Energy 的变化趋势；
- 用户主动选择的 mood labels 变化；
- 经常一起出现的压力来源；
- 睡眠、身体感受和情绪的共现，但不声称因果；
- 哪些人或事情常带来支持；
- 哪些应对方式曾被用户认为有帮助或没帮助；
- 哪些事项反复出现但尚未解决；
- 状态变好或变差之前，日记中出现了哪些可见变化；
- 哪些重要事件值得以后温和跟进。

### 10.3 不可以做什么

- 不从日记自动给出精神或身体疾病诊断；
- 不输出“你患有……”或“你属于……”；
- 不把词频直接解释为病情；
- 不把模型置信度包装成医学概率；
- 不在用户不知情时生成隐藏的敏感画像；
- 不把一篇日记的状态永久固化为用户特征；
- 不把长期观察提供给第三方；
- 不用长期观察替代专业评估。

### 10.4 呈现方式

好的表达：

> 最近两周的日记里，你有 4 次提到工作交接带来的疲惫。周末和朋友见面后，你给自己的能量评分比前几天高一些。这只是从你记录的内容中整理出的变化，不一定代表全部状态。

不好的表达：

> 系统判断你的抑郁程度正在加重，社交活动能够治疗你的症状。

每条观察必须附带：

- 时间窗口；
- 使用了多少篇日记；
- 来源日记链接；
- “用户记录”与“系统整理”的区别；
- 是否存在数据不足；
- 用户的“准确 / 不准确 / 不想看到”反馈。

### 10.5 未来价值

在未来、获得用户明确授权并完成更严格的隐私与合规设计后，MORA 可以把长期观察整理成一份用户拥有的个人状态摘要，供用户自己回顾，或由用户主动选择分享给可信任的人或专业支持者。

这份摘要的价值是节省回忆和整理成本，不是替用户下结论。未来进入长期状态观察阶段时，只验证数据结构、可解释性和用户体验，不实现医疗协作或第三方分享；当前纯文字 Web Demo 暂不实现该模块。

---

## 11. Memory 与 Context 的关系

不得把全部历史日记塞进每一轮 prompt。正确流程是：

```text
语音日记
   ↓
确认稿
   ↓
结构化提取
   ↓
用户确认的 Memory / Observation Evidence
   ↓
按当前对话需要选择少量相关信息
   ↓
Context Builder
   ↓
模型回复
```

Context 中应优先使用短小、明确、与当前对话相关的结构化记忆。日记原文只在用户明确要求回看、总结或引用该日记时使用。

当 MORA 基于日记主动跟进时，应避免令人不安的精准复述。例如：

推荐：

> 你前几天提过这周有个挺费神的交接。今天还好吗？不想展开也没关系。

避免：

> 你在 8 月 17 日 22:43 的录音中说领导在会议室批评了你 14 分钟。

---

## 12. 数据控制与信任

用户对记忆和日记的控制不是设置页附加项，而是 MORA 产品可信度的一部分。

必须做到：

- 录音前明确授权；
- 明确哪些内容会发送给哪个模型供应商；
- 转写稿可编辑；
- 结构化提取可修改；
- 长期记忆单独确认；
- 每条记忆显示来源；
- 每条观察可回到证据；
- 可删除单篇日记；
- 可选择是否连带删除派生记忆和观察证据；
- 可关闭原始音频保存；
- 可清空所有日记、记忆和观察；
- API Key 永远不进入浏览器或本地浏览器存储；
- 当前本地 Demo 的用户数据只保存在项目本机目录。

默认值应偏向少保存：

- 原始音频：默认不永久保存；
- 未确认 Memory：不进入长期记忆；
- 未确认观察：可以展示为草稿，不作为稳定用户事实；
- 原始供应商响应：默认不保存；
- 后台录音：永远禁止。

任何 LLM 调用在重试策略结束后仍失败时，用户界面必须明确显示“调用失败”。禁止使用预设回复、规则生成回复、缓存旧回复或其他模型回复作为保底。失败是实验结果的一部分，必须如实保留。

模型已经返回的生成内容绝对不允许在应用层硬截断、切片或改写。若供应商因自身输出 token 上限返回不完整内容，系统应完整保存其实际返回文本并标记结束原因，不得伪造完整回复。界面可以折叠长内容，但必须允许查看和复制全文。

---

## 13. 核心使用场景

### 场景 A：一句话陪伴

用户：

> 我今天真的没电了。

MORA：

> 嗯……那今天先省着用。我陪你趴一会儿。

体验重点：短、不追问、不立即给任务。

### 场景 B：用户想说，但不想打字

用户打开语音日记，断断续续说了三分钟。MORA 完成转写，用户修正了一个人名。系统给出短摘要和三条记忆候选，用户只确认其中一条。

体验重点：表达自由、解释可改、记忆可控。

### 场景 C：温和跟进

用户前几天确认“周五有重要面试”。周五之后再次聊天，MORA 可以轻轻问一句，但允许用户跳过。

体验重点：记得，但不监视；主动，但不逼迫。

### 场景 D：周期回顾

用户打开“最近的变化”，看到两周内工作压力被多次提到，同时有两篇日记记录了散步后稍微轻松。所有观察都能点回原日记。

体验重点：帮助用户看见变化，不提供诊断或因果断言。

### 场景 E：明确风险

当用户表达明确自伤计划或即时危险时，MORA 停止普通树懒话术，使用简短、直接的安全回应，鼓励联系当地紧急服务、可信任的人或专业支持。

体验重点：不夸张、不拖延、不继续角色表演。

---

## 14. 产品非目标

MORA 当前不追求：

- 成为心理百科；
- 每次对话都使用 CBT、DBT 或正念术语；
- 让用户每天完成固定打卡任务；
- 最大化聊天时长或消息数量；
- 通过情感绑定制造依赖；
- 自动诊断或形成不可见的风险评分；
- 代替医生、治疗师或紧急服务；
- 将所有用户数据永久保存；
- 用“AI 很懂你”掩盖错误推断；
- 在当前 Web Demo 中完成最终硬件、实时语音对话或 TTS。

---

## 15. 产品评估维度

### 15.1 陪伴体验

- 是否温和自然；
- 是否减少说教；
- 是否避免过度分析；
- 是否像 MORA，而不是通用助手；
- 用户是否愿意继续说；
- 用户是否可以舒服地结束对话。

### 15.2 低电量适配

- 回复是否符合目标长度；
- 是否减少提问；
- 是否减少任务和建议；
- 是否尊重“不想说”和“不想做”；
- 是否能在“什么都不逼”和“完全放弃支持”之间保持平衡。

### 15.3 语音日记

- 用户是否能低负担完成一篇日记；
- 转写准确度与人工修改量；
- 摘要是否忠实；
- 结构化字段是否准确且不过度推断；
- Memory 候选接受、修改和拒绝比例；
- 用户是否理解日记、记忆和观察之间的关系；
- 删除和撤销是否真正生效。

### 15.4 长期观察

- 每条观察是否有证据；
- 是否清楚区分用户记录与系统解释；
- 用户认为观察“准确、有帮助、不冒犯”的比例；
- 错误关联、伪因果和标签化的发生率；
- 用户能否纠正并影响后续观察；
- 是否避免诊断化语言。

### 15.5 技术与商业

- token、语音转写和单用户成本；
- 对话与转写延迟；
- provider 稳定性；
- Memory 召回准确性；
- 数据可删除性；
- 模型切换后的体验稳定性；
- 供应商依赖风险。

不要把“用户聊得更久”单独当作成功指标。对低电量用户而言，一次短而舒服的交互可能比长对话更成功。

---

## 16. Cursor 实现时的产品判断顺序

遇到本文件没有覆盖的细节时，按以下顺序判断：

1. 是否减少目标用户的交互负担；
2. 是否保留用户对日记、记忆和观察的控制；
3. 是否让系统解释可见、可改、可撤销；
4. 是否保持 MORA 的陪伴人格，而不是心理咨询口吻；
5. 是否避免诊断、标签化和未经授权的敏感推断；
6. 是否有利于实验比较和后续评估；
7. 是否符合本地 Demo 的简单技术边界。

如果产品体验与“多做一个智能功能”冲突，优先选择更克制、更透明的体验。

---

## 17. 当前版本的产品结论

MORA 的核心不是某一个大模型，也不是一套心理学话术。真正的产品资产是：

- 树懒 Persona；
- 低电量 Interaction Policy；
- 用户可控的结构化记忆；
- 以语音日记为重点入口的长期数据链路；
- 可追溯、非诊断化的状态观察；
- 安全边界；
- 面向真实目标用户的评估体系。

当前 MORA Lab 应优先证明两件事：

1. OpenAI 与 DeepSeek 中，哪一供应商及其模型参数更适合提供理想的中文低负担陪伴体验；
2. 纯文字聊天能否在不越过隐私和解释边界的前提下，形成属于单个测试档案、可确认且可控的结构化长期记忆。

当前不建立标准测试话术或理想回复基准集，也不把人工评分系统作为首版交付重点；先保留稳定的 run、上下文快照和后续评估接口。上述两件事成立后，MORA 才有理由继续验证语音日记、状态轨迹，并从一个“会聊天的树懒”成长为一个“长期认识用户、但仍尊重用户边界的陪伴伙伴”。
