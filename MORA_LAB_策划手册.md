# ZHAKA Lab 策划使用手册

> 给谁看：产品 / 策划 / 测试同学，用来理解「系统怎么决定这一轮怎么回」，以及「设置里改什么会影响什么」。  
> 不需要会写代码。遇到英文术语，文中都会跟中文释义。

---

## 1. ZHAKA Lab 是什么

**ZHAKA**（树懒陪伴伙伴）的产品目标是：在用户累、焦虑、没力气时，用**低负担**的对话陪着，而不是急着分析、说教或布置任务。

**ZHAKA Lab**（实验室）是给内部调试用的实验台，不是最终给用户的 App。你在这里可以：

- 调人格、能量、世界观、提问节奏等行为；
- 同一句话并行比较多个模型的回复（**Compare** 对比页）；
- 改完设置立刻试聊，看 Run（运行记录）里「这一轮到底按什么计划回复的」。

一句话记住：

> **Lab 里改的是「行为规则」；模型只是按规则把话说出来。**

---

## 2. 系统怎么决定「这一轮怎么回」（必读）

用户在 **Compare**（对比聊天）或相关试聊页发出一句话后，系统大致按下面顺序工作。  
理解这个顺序，再改设置就不会「改了 A 却以为在管 B」。

```text
用户这句话
    │
    ▼
① Safety（安全检查）
    │  命中紧急风险 → 直接给固定安全文案，后面几乎全跳过
    ▼
② Router（轻量路由）
    │  判断：能量档、回复模式、要不要问、要不要世界观、有没有大事…
    ▼
③ Turn Plan（本轮回复计划）
    │  把上面的判断编译成「本轮必须做什么 / 不要做什么 / 字数 / 问不问」
    │  其中：提问数量由 Question Policy（提问策略）拍板
    ▼
④ Worldview（世界观）定稿
    │  决定本轮是 W0 / W1 / W2 / W3，可能塞入种子或事实
    ▼
⑤ Context（上下文）组装
    │  人格 + 本轮计划 + 记忆 + 历史 + 用户原话 → 发给模型
    ▼
⑥ 模型生成回复
    ▼
⑦ Memory（记忆）检索 / 可选自动提取
```

### 2.1 每一步在决定什么

| 步骤 | 英文 | 中文 | 决定什么 |
|------|------|------|----------|
| ① | Safety | 安全检查 | 有没有自伤 / 暴力等风险；紧急时是否走占位回复 |
| ② | Router | 路由 | **能量档**、**回复模式**、**提问偏好**、**世界观关系**、是否大事等 |
| ③ | Turn Plan | 本轮回复计划 | 字数句数、动作上限、必做/禁做、**本轮问不问（0 或 1 问）** |
| ④ | Worldview | 世界观 | 本轮是否露出「雨林 / 朋友」等角色化内容，以及强度 |
| ⑤ | Context | 上下文 | 模型实际读到哪些分区、历史多长、记不记得旧事 |
| ⑥ | Model | 模型 | 在既定计划下把话说出来（语气仍受 Persona 人格影响） |
| ⑦ | Memory | 记忆 | 本轮塞进哪些旧记忆；聊完是否自动抽新记忆 |

### 2.2 几个核心概念（后面设置都绕不开）

#### 能量档 Energy（能量）

表示「用户这一轮像几格电」。档位主要由 **Router（路由）** 判断，影响**篇幅、动作多少、世界观强度上限**。

| 档位 | 含义（策划理解） |
|------|------------------|
| **E0** | 几乎没电 / 崩溃：极短、少动作，世界观最多轻轻一句 |
| **E1** | 低电量 |
| **E2** | 一般 |
| **E3** | 有余力：可以稍长；用户明确要求「详细说说」时还可再拉长 |

#### 回复模式 Response Mode（回复模式）

表示「这一轮该用哪种说话任务」。由 **Router（路由）** 选定，再套用对应策略。

| 模式 | 中文 | 大致干什么 |
|------|------|------------|
| **COMPANION** | 陪伴 | 接住用户说的具体事，不分析、没求就别建议 |
| **ASK_LIGHT** | 轻问 | 接住后，把话头轻轻递回去 |
| **DIRECT_ANSWER** | 直接回答 | 先答用户明确问的问题 |
| **ONE_STEP_HELP** | 单步帮助 | 给一个最小、能执行的一步 |
| **CONFIRM_CHOICE** | 确认选择 | 只确认用户已经做的决定 |
| **CELEBRATE** | 庆祝 | 具体地为已经发生的好事高兴 |
| **REPAIR** | 修复 | 认错并立刻调整 / 重答 |
| **CLOSE** | 结束 | 允许对话自然结束，别挽留、别加戏 |

> 策略里的「必须做 / 不要做」文案是**产品定死的规则**（代码常量），Lab 里只能改旁边的**数字旋钮**（字数倍率、动作上限、是否允许世界观），不能在界面里改那些长文案。

#### 提问偏好 Question Preference（提问偏好）

也是 Router 先判一档，再由 **Question Policy（提问策略）** 落成「本轮最多几个问号」：

| 偏好 | 中文 | 结果 |
|------|------|------|
| **invite** | 邀请提问 | 用户像在邀请你问 → **必问 1 个** |
| **avoid** | 避免提问 | 不宜问 → **0 问** |
| **neutral** | 中性 | 由你在设置里配的「neutral 模式 / 概率」决定 **0 或 1 问** |

全产品硬规矩：**每一轮最多 1 个问句**，没有「可以问很多」这种档。

#### 世界观强度 Worldview Mode（世界观模式）

| 模式 | 中文 | 表现 |
|------|------|------|
| **W0** | 不显性世界观 | 这一轮不专门带出角色化故事 |
| **W1** | 一句平行感受 | 用一两句「我这边也有类似感觉」轻轻陪衬 |
| **W2** | 一小段平行感受 | 稍长一点的平行感受（仍受篇幅占比限制） |
| **W3** | 直接答设定 | 用户在追问「你从哪来 / 你朋友是谁」等 → 用事实库回答 |

用户对世界观的态度叫 **Worldview Relation（世界观关系）**：

- **required（必须回应）** → 走 W3  
- **eligible（可自然带入）** → 由调度器决定要不要 W1/W2  
- **discouraged（拒绝角色化）** → 强制 W0  

---

## 3. 你平时在哪改配置

| 地方 | 改什么 |
|------|--------|
| **设置** 页 | 行为配置 v2、模型槽位、供应商参数、记忆、上下文预算等（底部统一 **「保存」**） |
| **Studio**（工作室）页 | **Persona（人格）**、**Prompt Preset（提示词预设）** 的长文案 |
| **Memory**（记忆）页 | 查看 / 改 / 删已经写入的记忆条目 |
| **Compare**（对比）页 | 试聊；可选本轮手动覆盖能量档（若设置里允许） |
| **Runs**（运行记录）页 | 回看某一轮：路由结果、本轮计划、有没有世界观、用了哪张示例卡 |

配置按 **测试档案 Profile** 隔离：换档案 = 换一套设置与数据，互不串。

**保存提醒：**

- 设置页底部只有一个 **「保存」**，会同时写入行为配置与模型 / Context 等设置。  
- 改完后应看到成功提示；若保存失败，页顶会出现红色错误（例如校验未通过），**刷新前请先确认已保存成功**。

---

## 4. 设置项详解：改什么 → 影响什么

下面按设置页区块说明。只写**策划需要关心、且改了真会影响运行**的项。

---

### 4.1 行为配置 v2（核心行为引擎）

这是 Compare / 试聊真正在读的「行为大脑」。  
改这里 = 改 **Router（路由）怎么判、字数怎么控、问不问、世界观多不多、安全怎么拦**。

#### 4.1.1 Brand Canon（品牌与世界观口径）

| 可改项 | 影响 |
|--------|------|
| 来源表述 / 抵达表述 | 品牌口径；保存时会做禁词检查 |
| 已批准物种列表 | 新写种子 / 事实时尽量落在白名单内，避免跑出非亚马逊设定 |

**不决定：** 某一轮具体回什么话（那是种子 / 事实 / 计划在管）。

#### 4.1.2 Turn Router（轻量路由）

| 可改项 | 影响 |
|--------|------|
| 是否启用、模式（llm 模型 / rules 规则 / fixed 固定） | 用不用小模型来判能量与模式；规则模式更稳但更死板 |
| 供应商、模型 | 路由判断用哪家小模型（与主聊天模型可以不同） |
| 最低置信度、超时 | 太不确定或太慢时会 **fallback（回退）** 到规则层，避免卡死 |

**决定：** 这一轮是 E几、用哪种 Response Mode、提问偏好是 invite/neutral/avoid、要不要世界观、有没有重大事件等。  
**不决定：** 问句最终 0 还是 1（neutral 时交给提问策略）；也不决定具体世界观句子内容。

#### 4.1.3 Question Policy（提问策略）——策划常调

| 可改项 | 影响 |
|--------|------|
| **neutralMode（neutral 模式）** | 仅当 Router 判成 **neutral（中性）** 时生效：`never` 一律不许问 / `must_ask` 一律必问 / `probabilistic` 按概率二选一 |
| **neutralMustAskProbability（必问概率）** | 仅 probabilistic：例如 `0.7` = 70% 必问、30% 不许问（输入界面是百分比） |
| **suppressIfLastAssistantAsked（上轮抑制）** | 若上一轮助手回复里已经有问句，本轮 neutral **强制 0 问**，避免连着审问 |

**决定：** neutral 轮次要不要问。  
**不决定：** invite / avoid（那是 Router 定的）；也不提高「每轮最多问几个」（永远最多 1）。

想少问：把概率调低，或开上轮抑制，或让 Router 更常判 avoid（需调路由模型 / 规则，不是改本区三个开关能完全做到）。

#### 4.1.4 Request Flags（请求标志）

界面多为**只读展示**。关键词命中后，系统知道用户在要「详细说说」等；在 **E3** 时可能把篇幅放到硬上限。  
**多步方案**类开关当前产品侧基本关闭，不必当主旋钮。

#### 4.1.5 Energy v2（四档预算）

每一档可调：目标字数区间、硬上限字数、最多几句、默认最多几个「动作」、模型输出 token 上限等；还有全局「超硬上限容忍比例」。

| 可改项 | 影响 |
|--------|------|
| 各档字数 / 句数 | Turn Plan 里的篇幅目标；主模型被要求按这个尺度写 |
| 默认动作上限 | 「帮你做一步」类动作的数量天花板（还会和策略数值取更严的） |
| providerMaxOutputTokens | 给供应商的输出上限参考 |

**决定：** 判成某一能量档之后，**写多长、写几句**。  
**不决定：** 本轮到底是 E0 还是 E3（那是 Router / 手动覆盖）。

> 设置页里若还有旧版 Energy 文案区：当前 Compare 主路径以 **v2 四档预算**为准；旧文案基本不驱动本轮计划。

#### 4.1.6 Major Event（重大事件）

| 可改项 | 影响 |
|--------|------|
| 是否启用、指纹相似度 / 保留月数 | 是否把「失去 / 哀恸」等大事当首轮特殊处理，以及去重 |
| 首轮字数 / 句数上限 | 大事刚提起时压短回复，先接住，不抢戏 |

大事首轮通常**不算**「可以自然塞世界观」的合格轮，避免一上来讲雨林故事。

#### 4.1.7 Response Strategies（策略数值）

每个回复模式旁可调：

| 可改项 | 影响 |
|--------|------|
| **lengthMultiplier（长度倍率）** | 在能量档字数基础上再乘一截（例如结束模式更短） |
| **defaultMaxActions（默认动作上限）** | 该模式下最多几个动作 |
| **allowWorldview（允许世界观）** | 关掉则该模式几乎不会进入「自然世界观」合格轮 |

界面上的 **goal / mustDo / mustAvoid（目标 / 必做 / 禁做）** 是只读说明，改不了；改了导入包也会被系统拉回代码常量。

#### 4.1.8 Worldview Scheduler（世界观调度）

控制「在可自然带入的轮次里，**多久露一次** W1/W2」。

| 可改项 | 影响 |
|--------|------|
| **organicTargetRate（自然出现目标比例）** | 长期大概多少合格轮会带自然世界观（默认约 25%） |
| 滚动窗口、最小 / 最大间隔 | 太密会压、太久没出现会补；避免机械「每四句来一次」 |
| 任意世界观之间的助手轮间隔 | W3 刚说过设定后，别立刻再塞平行感受 |
| 种子冷却轮数 | 同一组故事别连着重复 |
| **maxWorldviewShare（世界观篇幅占比）** | W2 时「角色化部分」最多占回复大约多少 |
| **forbiddenLegacyTerms（废弃设定禁词）** | 保存事实 / 种子 / 示例时扫禁词（如旧海岸地名），防止设定漂移 |

**决定：** 合格轮里要不要调度出 W1/W2、频率与间隔。  
**不决定：** 用户明确追问设定时的 W3（那是 required → 事实库）；也不决定种子里具体写什么句子。

#### 4.1.9 Canon Facts（世界观事实库）

给 **W3** 用：用户问「你从哪来」「你朋友是谁」等时检索。

| 可改项 | 影响 |
|--------|------|
| 内容、别名、启停 | 命中别名则把事实注入本轮计划，模型按事实答，不瞎编 |

#### 4.1.10 Worldview Seeds（世界观种子库）

给 **W1 / W2** 用：自然陪衬时的「记忆片段 + 态度」。

| 可改项（界面常见） | 影响 |
|--------------------|------|
| 标题、触发说明、记忆、态度 | 调度命中后，作为「平行感受」素材进入计划（措辞由模型组织，不是固定台词） |
| 冷却组、启停 | 控制重复与是否参与候选 |

进阶字段（部分在数据里）：允许哪些回复模式、能量档、只要 W1 还是也可 W2、避开的大事类型、关联事实 id 等——决定**什么情况下这颗种子能被选中**。

#### 4.1.11 Behavior Examples（行为示例卡）

给模型看的「这种模式大概长这样」的示范对话（口吻参考）。

| 可改项 | 影响 |
|--------|------|
| 用户句 / 理想回复、适用模式与能量、启停与审核状态 | 本轮可能检索到一张卡塞进计划 |
| 检索开关、最低分、最大字数 | 严一点就更少注入；关了就不注入 |

注意：本轮若已经是 **W1 / W2**，示例卡通常**让位**，避免和种子抢戏。

#### 4.1.12 Safety Rules（安全规则）

| 可改项 | 影响 |
|--------|------|
| 关键词、否定词、级别（concern 需关注 / urgent 紧急）、启停 | 命中后走澄清压缩或**紧急占位回复**（紧急时可跳过主模型） |

紧急占位文案一般当产品固定文案看待；改关键词时务必小心误伤日常口语（所以有否定词，如「笑死」「困死了」）。

#### 4.1.13 Response Contract（回复契约）

多为**只读**：禁止标题、列表、内部分析、角色标签；emoji 上限等。  
保证输出像聊天，不像报告。

---

### 4.2 模型槽位与供应商（不影响「行为判断」，只影响「谁来写、怎么生成」）

| 区块 | 可改项 | 影响 |
|------|--------|------|
| **模型槽位** | 启用、显示名、供应商、模型 id、单槽覆盖参数 | Compare 里有几列在比；改模型建议新开对话，避免历史串味 |
| **供应商与生成参数** | temperature、max tokens、推理强度、超时重试等 | 文风随机度、长度上限、延迟；**不改** Router 判的模式 / 提问策略 |

可以把行为想成「导演的分镜」，模型想成「演员」：分镜不变，换演员台词感觉会变，但场次结构仍由分镜决定。

---

### 4.3 Energy 手动覆盖（Compare 用）

| 可改项 | 影响 |
|--------|------|
| 允许本轮手动覆盖 | Compare 里可以强制指定 E0–E3，方便专项试「没电时够不够短」 |

覆盖的是**档位**；字数预算仍读 v2 四档表。

---

### 4.4 Memory（记忆选择与自动提取）

| 可改项 | 影响 |
|--------|------|
| 是否启用、取几条、字数上限、最低重要度、类型、权重 | **本轮**塞进上下文的旧记忆有哪些 |
| 自动提取：开关、用哪个模型、是否要人工确认 | 聊完后是否从用户话里抽候选记忆、要不要你点头才入库 |

**决定：** 长期关系感、是否记得用户说过的事。  
**不决定：** 本轮问不问、要不要世界观（那是行为配置）。

---

### 4.5 Context（上下文预算）

| 可改项 | 影响 |
|--------|------|
| 历史轮数、历史字数、总字数 | 模型能「看见」多长的近期对话；太紧会裁掉旧轮 |
| 分区顺序、是否带时间戳 / 能量原因 / 记忆元数据 | 提示词结构（v2 托管的本轮计划等分区不受旧模板随意改写） |

**决定：** 模型读到的材料多寡与顺序。  
**不决定：** Turn Plan 里的行为规则本身。

---

### 4.6 日志与容量

| 可改项 | 影响 |
|--------|------|
| 每个档案保留的 Run 条数上限 | 超了删最旧的运行记录；调试回放空间 |

上下文快照等调试信息按产品约定一律保存，没有「关日志就不记」的开关。

---

### 4.7 配置导入 / 导出

可导出整包行为配置，或单独导出世界观库 / 示例库；完整导出还可带上 Lab 运行时（设置、人格、预设）。  
用于备份、换机器、给同事同一套行为基线。  
导入会做校验；策略长文案仍会被系统拉回只读常量。

---

### 4.8 Studio 里的 Persona / Preset（不在设置折叠区，但策划常改）

| 对象 | 影响 |
|------|------|
| **Persona（人格）** | 核心人设、关系框架、边界、风格偏好 → 进入 Context 的人格 / 风格分区，管「像不像 ZHAKA」 |
| **Prompt Preset（提示词预设）** | 各分区模板；其中本轮计划、回复契约、安全底线等已被 v2 **托管覆盖**，不要在预设里再写一套「每轮必须提问」之类，以免和 Turn Plan 打架 |

提问、字数、世界观频率：**以行为配置 v2 + Turn Plan 为准**，不要靠人格长文重复立法。

---

## 5. 策划调参速查（「我想…」）

| 我想… | 优先改哪里 |
|--------|------------|
| 中性轮少问一点 | Question Policy：降低必问概率，或打开上轮抑制 |
| 中性轮几乎不问 | neutralMode = never |
| 中性轮几乎总问 | neutralMode = must_ask（仍受「上轮已问则抑制」约束） |
| 回复整体更短 / 更长 | Energy v2 各档字数；或策略 lengthMultiplier |
| 少出现雨林 / 朋友故事 | 降低 organicTargetRate，或加大最小间隔；或相关模式关掉 allowWorldview |
| 用户问设定却答不准 | 补 / 改 Canon Facts 与 aliases |
| 自然世界观内容乏味或跑题 | 改 Seeds 的 memory / attitude / 触发；检查启停与冷却 |
| 口吻不像示范 | 加 / 改 Example Cards（注意 W1/W2 时可能不注入） |
| 人格不像树懒 | Studio 改 Persona |
| 比两个模型谁更贴 | 模型槽位启用两列，行为配置保持同一套 |
| 模拟「用户没电」 | Compare 开能量覆盖 → 选 E0/E1 |
| 紧急安全话术是否触发 | Safety 关键词 / 否定词（务必回归测日常口语） |

---

## 6. 建议不要当「旋钮」乱拧的东西

| 内容 | 原因 |
|------|------|
| 策略里的 goal / mustDo / mustAvoid 长文案 | 代码常量；导入也会被拉回 |
| 「每轮最多问几个」想调成 2+ | 产品硬上限就是 1 |
| 在 Persona / Preset 里再写一套提问 / 字数立法 | 会和 Turn Plan 双头政治，难排查 |
| 禁词表里的词本身 | 表的职责就是列出禁词；不要删表来「绕过」校验 |
| 随意清空测试档案数据 | 对话、记忆、运行记录按档案隔离，清错难复现 |

---

## 7. 改完怎么验收（推荐习惯）

1. 设置页改完 → 点底部 **「保存」** → 确认成功提示。  
2. 到 **Compare** 用固定几句测例试聊（例如：闲聊、明确提问、低能量倾诉、追问「你从哪来」）。  
3. 打开该轮 **Run / 检视面板**，核对：  
   - Energy（能量档）  
   - Response Mode（回复模式）  
   - Question Preference + 实际 maxQuestions（问不问）  
   - Worldview Mode（W0–W3）与是否注入种子 / 事实  
4. 若「界面改了刷新又变回去」：先看保存是否报错；行为项必须进 **behavior-config（行为配置）**，不是只改模型设置就能生效。

---

## 8. 术语小词典（英文 → 中文）

| 英文 | 中文 |
|------|------|
| ZHAKA Lab | ZHAKA 实验室 / 实验台 |
| Profile | 测试档案 |
| Compare | 多模型对比聊天 |
| Studio | 人格与提示词工作室 |
| Run / Run Inspector | 运行记录 / 运行检视 |
| Behavior Config | 行为配置 |
| Router | （轻量）路由 |
| Safety | 安全检查 |
| Energy | 能量（档） |
| Response Mode | 回复模式 |
| Strategy | 策略 |
| Question Preference | 提问偏好 |
| Question Policy | 提问策略 |
| Turn Plan | 本轮回复计划 |
| Worldview | 世界观 |
| Canon Fact | 世界观事实 |
| Seed | 世界观种子 |
| Example Card | 行为示例卡 |
| Context | 上下文（发给模型的整包材料） |
| Persona | 人格 |
| Prompt Preset | 提示词预设 |
| Memory | 记忆 |
| Fallback | 回退（路由失败时用规则顶上） |
| Provider | 模型供应商 |
| Slot | 模型槽位 |
| Token | 模型计费/长度单位（可粗理解为「字词额度」） |
| LLM | 大语言模型 |
| Config Hash | 配置指纹（用来解释「行为是不是同一套配置」） |

---

## 9. 一句话总览

> **Router 决定这一轮「是什么场」；Question Policy 决定中性场「问不问」；Energy 决定「写多长」；Worldview 调度决定「要不要露角色」；Persona 决定「像不像 ZHAKA」；模型只负责把计划说成人话。**

有疑问时，优先看某一轮 Run 里的路由结果与 Turn Plan，而不是只盯最终回复猜原因。

---

*文档对应当前 ZHAKA Lab 行为配置 v2 主路径；若界面文案与本文不一致，以 Lab 内最新标签与保存成功后的实际 Run 为准。*
